Example

Input: Python
Output: ['P', 'y', 't', 'h', 'o', 'n']


-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

def stringToList(s):
    result = []

    for char in s:
        result.append(char)

    return result


# ===== MAIN PROGRAM =====
text = input("Enter a string: ")

print("Character list:", stringToList(text))
